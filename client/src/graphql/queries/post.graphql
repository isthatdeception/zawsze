query Post($id: Int!) {
  post(id: $id) {
    id
    createdAt
    updatedAt
    title
    text
    creatorId
    voteStatus
    zpoints
    creator {
      id
      username
      email
      createdAt
      updatedAt
    }
  }
}
